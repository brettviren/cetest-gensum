{% set reltoroot = '..' %}

{% include 'header.html.j2' %}
    

<script type="text/javascript" class="init">
  $(document).ready(function() {
  $('#summary').DataTable();
  } );
  </script>

<h1>All FE ASIC Test Result Summaries</h1>


<table id="summary" class="display" width="100%" cellspacing="0">
  <thead>
    <tr>
      <th>Serial</th>
      <th>Timestamp</th>
      <th>Board</th>
      <th>Config</th>
    </tr>
  </thead>
  <tbody>
    {% for ident, one in index.items() %}
    <tr>
      <td><a href="{{ reltoroot }}/feid/{{ one.serial }}">{{ one.serial }}</a></td>
      <td><a href="board{{ one.board }}/{{ one.timestamp }}">{{ one.timestamp }}</a></td>
      <td><a href="board{{ one.board }}">{{ one.board }}</a></td>
      <td>{{ one.femb_config }}</td>
    </tr>
    {% endfor %}
  </tbody>
</table>



{% include 'footer.html.j2' %}
    
