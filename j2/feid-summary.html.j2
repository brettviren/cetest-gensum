{% include 'header.html.j2' %}
    
<script type="text/javascript" class="init">
  $(document).ready(function() {
  $('#summary').DataTable();
  } );
  </script>

<h1>All test results involving FE# {{ feid.ident }}</h1>

<h2>Notes</h2>

The results table below includes all tests in which FE# {{ feid.ident }} participated.  The meaning of <b>Board ID</b> depends on the test:

<ul>
  <li><b>feasic</b> the FE ASIC test board ID</li>
  <li><b>femb</b> the FE motherboard ID</li>  
</ul>

Board ID links go to a directory of all results in which that board
particpated.  Timestamp links go to a specific result.

<h2>Results</h2>

<table id="summary" class="display" width="100%" cellspacing="0">
  <thead>
    <tr>
      <th>Type</th>
      <th>Board ID</th>
      <th>timestamp</th>
    </tr>
  </thead>
  <tbody>
    {% for one in feid.feasic -%}
    <tr>
      <td>feasic</td>
      <td><a href="{{ reltoroot }}/feasic/board{{ one.board_id}}">{{ one.board_id }}</a></td>
      <td><a href="{{ reltoroot }}/feasic/{{ one.path }}">{{ one.timestamp }}</a></td>
    </tr>
    {% endfor -%}

    {% for one in feid.femb -%}
    <tr>
      <td>femb</td>
      <td><a href="{{ reltoroot }}/femb/femb-{{ one.board_id}}">{{ one.board_id }}</a></td>
      <td><a href="{{ reltoroot }}/femb/{{ one.path }}">{{ one.timestamp }}</a></td>
    </tr>
    {% endfor -%}
  </tbody>
</table>

{% include 'footer.html.j2' %}
    
