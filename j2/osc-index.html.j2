{% set reltoroot = '..' %}

{% include 'header.html.j2' %}
    

<script type="text/javascript" class="init">
  $(document).ready(function() {
  $('#summary').DataTable();
  } );
  </script>

<h1>All Oscillator Pre-screening Test Result Summaries</h1>


<table id="summary" class="display" width="100%" cellspacing="0">
  <thead>
    <tr>
      <th>Timestamp</th>
      <th>Config</th>
      <th>DAQ host</th>
      <th>Code version</th>
    </tr>
  </thead>
  <tbody>
    {% for ident, one in index.items() %}
    <tr>
      <td><a href="{{ one.timestamp }}">{{ one.timestamp }}</a></td>
      <td>{{ one.femb_config }}</td>
      <td>{{ one.hostname }}</td>
      <td><a href="https://github.com/DUNE/femb_python/releases/tag/{{ one.version }}">{{ one.version }}</a></td>
    </tr>
    {% endfor %}
  </tbody>
</table>



{% include 'footer.html.j2' %}
    
